@model IEnumerable<Tidrapport.Models.TimeReportTemplate>

@{
    ViewBag.Title = "Index";
}

<h2>Tidrapportmall</h2>

@*<p>
    @Html.ActionLink("Skapa ny tidrapportmall", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Activity.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.SSN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DayOfWeek)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfHours)
        </th>
        <th>
            <a href="@Url.Action("Create")" class="btn btn-default btn-">
                <span class="glyphicon glyphicon-plus-sign"></span>
                Skapa ny tidrapport-mall
            </a>
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Activity.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.SSN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DayOfWeek)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumberOfHours)
        </td>
        <td>
            <span>
                <a href="@Url.Action("Edit", new { id = item.Id })" class="btn btn-default btn-sm">
                    <span class="glyphicon glyphicon-edit"></span> Editera
                </a>
            </span>
            @*<span>
        <a href="@Url.Action("Details", new { id = item.Id })" class="btn btn-default btn-sm">
            <span class="glyphicon glyphicon-expand"></span> Detaljer
        </a>
        </span>*@
            <span>
                <a href="@Url.Action("Delete", new { id = item.Id })" class="btn btn-default btn-sm">
                    <span class="glyphicon glyphicon-trash"></span> Tabort
                </a>
            </span>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
        </td>
    </tr>
}
</table>
<div>
    <a href="@Request.UrlReferrer" class="btn btn-default btn-md-10" title="">
        <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span> Tillbaka
    </a>
</div>
